#include <iostream>
#include "Utils.h"

// This file is just for compilation at the moment, and does not work as required.

void reverse(int* nums, unsigned int size)
{
    int x = 0;
}

int* reverse10()
{
    int x = 0;
    int* y = &x;
    return y;
}
